#pragma once

namespace calc {

// the one number type the whole calculator uses. behind an alias so it can be
// swapped later without touching every signature.
using Number = double;

} // namespace calc
